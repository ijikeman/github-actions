on: [push]

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - name: dotenv
        uses: actions/dotenv-action@v1.1.2

      - name: printenv test
        run: printenv

  call-workflow:
    uses: ./.github/workflows/call2.yml
    with:
      ENV_FILE: .env.prd
