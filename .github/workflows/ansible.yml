---
name: 'Ansible'

on:
  workflow_call:
    inputs:
      CD_FILE:
        type: string
        required: true
  
jobs:
  ansible_job:
    runs-on: ubuntu-20.04
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: 
        run: cat ./${{ inputs.CD_FILE }}
