name: Reusable workflow A

on:
  workflow_call:
    inputs:
      param1:
        description: 'Param1'
        required: true
        type: string

jobs:
  test-job:
    runs-on: ubuntu-20.04
    steps:
      - name: test
        run: echo ${{ inputs.param1 }}
